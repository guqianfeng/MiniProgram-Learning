<!--miniprogram/pages/books/books.wxml-->
<view wx:for="{{booklist}}" wx:key="_id">
  <image src="{{item.cover_url}}" />
  <text>{{item.title}}</text>
  <view>{{item.intro}}</view>
  <!-- <view>{{item.tags}}</view> -->
</view>
