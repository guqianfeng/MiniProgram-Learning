<!--miniprogram/pages/books/books.wxml-->
<view wx:for="{{booklist}}" wx:key="_id" class="book-info">
  <image src="{{item.cover_url}}" />
  <view class="book-title">{{item.title}}</view>
  <view>{{item.intro}}</view>
  <!-- <view>{{item.tags}}</view> -->
</view>
